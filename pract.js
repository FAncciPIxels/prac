
function clicked() {
    let subAr = ["red", "green", "yellow", "black", "grey", "purple", "blue"]
    let inputed = document.getElementById("inputed").value;
    //sub =document.getElementById("sub");
    var outPuted  =document.getElementById("outPut"),
    neg  = document.getElementById("neg");
    let r = 150;
   // let atrr = document.createAttribute("style");
    outPuted.style.backgroundColor = inputed;    
    outPuted.classList.add("show")
     if (outPuted.style.background = ) {
        outPuted.classList.remove("show")
     }
     else {
        outPuted.classList.add("show")
     }

     
}